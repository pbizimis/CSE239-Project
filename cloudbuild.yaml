steps:
  - name: 'gcr.io/cloud-builders/docker'
    id: 'build-server'
    args: ['build', '-t', 'gcr.io/$PROJECT_ID/olympis-server:$COMMIT_SHA', '.']
    dir: 'olympis-server'
    waitFor: ['-']

  - name: 'gcr.io/cloud-builders/docker'
    id: 'build-crawler'
    args: ['build', '-t', 'gcr.io/$PROJECT_ID/olympis-crawler:$COMMIT_SHA', '.']
    dir: 'olympis-crawler'
    waitFor: ['-']

  - name: 'gcr.io/cloud-builders/docker'
    id: 'build-agents'
    args: ['build', '-t', 'gcr.io/$PROJECT_ID/olympis-agents:$COMMIT_SHA', '.']
    dir: 'olympis-agents'
    waitFor: ['-']

  - name: 'gcr.io/cloud-builders/docker'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        docker push gcr.io/$PROJECT_ID/olympis-server:$COMMIT_SHA
        docker push gcr.io/$PROJECT_ID/olympis-crawler:$COMMIT_SHA
        docker push gcr.io/$PROJECT_ID/olympis-agents:$COMMIT_SHA
    waitFor: ['build-server', 'build-crawler', 'build-agents']

options:
  logging: CLOUD_LOGGING_ONLY

images:
  - 'gcr.io/$PROJECT_ID/olympis-server:$COMMIT_SHA'
  - 'gcr.io/$PROJECT_ID/olympis-crawler:$COMMIT_SHA'
  - 'gcr.io/$PROJECT_ID/olympis-agents:$COMMIT_SHA'
